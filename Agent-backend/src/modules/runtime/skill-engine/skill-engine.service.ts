import { Injectable } from '@nestjs/common';

@Injectable()
export class SkillEngineService {
  // V2 placeholder.
  // V1 does not execute tool skills. Agent capability and behavior are expressed in manifest.config.prompt.
}
